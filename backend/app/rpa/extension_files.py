from __future__ import annotations

EXTENSION_FILES = ("etax_extra_income_reports.py",)

# Extension hashes may be pinned for a release build. During source development an
# empty mapping keeps extension code editable without weakening vendor protection.
EXTENSION_SHA256: dict[str, str] = {}
